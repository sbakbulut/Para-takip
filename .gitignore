# Yerel / gecici dosyalar
*.tmp
*.bak
*~
.DS_Store
Thumbs.db

# API anahtari iceren yerel dosyalar — ASLA commit edilmez
# (publish.sh `git add -A` kullanir; anahtar sizmasin diye burada kapali)
openrouter*.txt
*api*key*.txt
*api-key*.txt

# Test / arac ciktilari
node_modules/
# NOT: package-lock.json BILEREK takip edilir (CI `npm ci` ile deterministik kurulum yapar)
/tmp-*

# Yedek dosyalari
para-kontrol-*.json
para-kontrol-*.csv
